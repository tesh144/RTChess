using UnityEngine;

namespace ClockworkGrid
{
    /// <summary>
    /// Soldier - Basic balanced unit.
    /// Stats are set as defaults in the inspector via the base Unit class.
    /// HP: 10, Attack: 3, Range: 1, Interval: 2, Cost: 3
    /// </summary>
    public class SoldierUnit : Unit
    {
        // Soldier-specific behavior will go here in future iterations.
        // For now, all behavior is inherited from Unit base class.
        // Stats are configured on the prefab via inspector.
    }
}
